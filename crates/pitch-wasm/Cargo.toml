[package]
name = "pitch-wasm"
description = "wasm-bindgen wrapper around pitch-core for the web tuner"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pitch-core = { path = "../pitch-core" }
pitch-song = { path = "../pitch-song" }
wasm-bindgen = "0.2"
